# Aptos Testnet Configuration
# Get testnet APT from: https://aptoslabs.com/faucet

# Private key for wallet (0x prefix required; generate via: aptos key generate)
PRIVATE_KEY=0x

# Deployed contract address (auto-populated by bun run deploy)
CONTRACT_ADDR=0x

# Aptos network (localnet | testnet | mainnet)
APTOS_NETWORK=testnet

# RPC endpoint (optional; defaults to official Aptos)
APTOS_RPC_URL=https://api.testnet.aptoslabs.com/v1
